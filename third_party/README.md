# Third-Party Dependencies

Place the public `lattice-estimator` checkout here when running the estimator
experiments locally:

```text
third_party/lattice-estimator/
```

The artifact expects commit `6019056` of
`https://github.com/malb/lattice-estimator.git`. The helper script
`scripts/setup_lattice_estimator.sh` creates this checkout.
